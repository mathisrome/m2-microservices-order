export class User {

    constructor(
        public id: number,
        public username: string,
        public email: string
    ) {
        this.id = id
        this.username = username
        this.email = email
    }

    
}